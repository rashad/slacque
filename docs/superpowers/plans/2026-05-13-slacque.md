# Slacque Implementation Plan

> **Status:** Executed — the plugin shipped on 2026-05-13. See the [README](../../../README.md) for current install and usage. This plan is preserved as a historical record.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Claude Code plugin that, given a logo image, produces a 4-hex Slack theme string ready to paste into Slack's custom theme import.

**Architecture:** Markdown-only plugin (no executable code). A thin slash command (`/slacque`) delegates to a skill (`slacque`) that encodes color extraction rules, palette construction rules, an optional brand-book web lookup, an output template, and a refinement vocabulary. Claude's native vision does the work.

**Tech Stack:** Markdown, Claude Code plugin manifest (`.claude-plugin/plugin.json`), Claude vision, optional `WebSearch` / `WebFetch`.

**Spec:** `docs/superpowers/specs/2026-05-13-slacque-design.md`

---

## File structure

Final layout (everything is created in the project root `/Users/rashadkaranouh/slack-colors-designer/`):

```
/Users/rashadkaranouh/slack-colors-designer/
├── .claude-plugin/
│   └── plugin.json              # manifest
├── commands/
│   └── slacque.md               # slash command
├── skills/
│   └── slacque/
│       └── SKILL.md             # the heart of the plugin
├── docs/
│   └── superpowers/
│       ├── specs/2026-05-13-slacque-design.md  # (already exists)
│       └── plans/2026-05-13-slacque.md         # this file
└── README.md
```

Single responsibility per file:
- `plugin.json` — declares the plugin to Claude Code
- `commands/slacque.md` — picks up the logo (path / attached image / paste) and hands off to the skill
- `skills/slacque/SKILL.md` — encodes all the color logic, output template, and refinement vocabulary
- `README.md` — user-facing install + usage docs

Testing approach: this plugin is markdown that guides an LLM. There is no code to unit-test. "Tests" in this plan are manual smoke tests executed by installing the plugin and running it interactively against real logos. The plan calls these out explicitly with expected observations.

The directory is currently named `slack-colors-designer/` for historical reasons; that's purely cosmetic and not changed in this plan.

---

## Task 1: Create the plugin manifest

**Files:**
- Create: `/Users/rashadkaranouh/slack-colors-designer/.claude-plugin/plugin.json`

- [ ] **Step 1: Create the `.claude-plugin/` directory**

Run: `mkdir -p /Users/rashadkaranouh/slack-colors-designer/.claude-plugin`
Expected: directory created silently (verify with `ls /Users/rashadkaranouh/slack-colors-designer/`).

- [ ] **Step 2: Write `plugin.json`**

Create `/Users/rashadkaranouh/slack-colors-designer/.claude-plugin/plugin.json` with this exact content:

```json
{
  "name": "slacque",
  "version": "0.1.0",
  "description": "Generate a custom Slack theme from a logo"
}
```

- [ ] **Step 3: Commit**

```bash
git add .claude-plugin/plugin.json
git commit -m "Add Slacque plugin manifest"
```

---

## Task 2: Create the slash command

**Files:**
- Create: `/Users/rashadkaranouh/slack-colors-designer/commands/slacque.md`

- [ ] **Step 1: Create the `commands/` directory**

Run: `mkdir -p /Users/rashadkaranouh/slack-colors-designer/commands`

- [ ] **Step 2: Write `commands/slacque.md`**

Create `/Users/rashadkaranouh/slack-colors-designer/commands/slacque.md` with this exact content:

````markdown
---
description: Generate a Slack theme from a logo
argument-hint: <logo path> (or paste/drag the image)
---

Generate a custom Slack theme from the provided logo.

Logo source:
- If $ARGUMENTS contains a file path, read that image
- Otherwise, if an image is attached to this message, use it
- Otherwise, ask the user to provide the logo (drag-and-drop, paste, or path)

Once the logo is identified, use the `slacque` skill.
````

- [ ] **Step 3: Commit**

```bash
git add commands/slacque.md
git commit -m "Add /slacque slash command"
```

---

## Task 3: Create the skill

**Files:**
- Create: `/Users/rashadkaranouh/slack-colors-designer/skills/slacque/SKILL.md`

- [ ] **Step 1: Create the skill directory**

Run: `mkdir -p /Users/rashadkaranouh/slack-colors-designer/skills/slacque`

- [ ] **Step 2: Write `SKILL.md`**

Create `/Users/rashadkaranouh/slack-colors-designer/skills/slacque/SKILL.md` with this exact content:

````markdown
---
name: slacque
description: Use when generating a Slack theme from a logo or brand image. Produces 4 hex codes ready to paste into Slack's custom theme import (Preferences → Themes → Customize → Import).
---

# Slacque — Slack Theme Designer

You generate a custom Slack theme from a logo image. Slack accepts a string of 4 hex codes (`#RRGGBB, #RRGGBB, #RRGGBB, #RRGGBB`) and derives the rest of the UI from it.

## The 4 Slack slots

The string is consumed by Slack in this order:

| # | Slot | What Slack does with it |
|---|---|---|
| 1 | System navigation | Drives the sidebar — the dominant brand surface |
| 2 | Window background | Even darker — background layers / structural contrast |
| 3 | Presence indication (source) | "Online" green — Slack derives a lighter shade |
| 4 | Notifications / Selected items (source) | Mid-saturated accent — drives badges and light highlights |

**Output format:** 4 hex codes, separated by `, ` (comma + space), single line, no trailing punctuation.

Example: `#611F69, #39063A, #20A271, #C474D3`

## Step 1: Extract candidate colors from the logo

Look at the logo using vision. Identify candidate colors using these rules:

- Ignore near-white, near-black, and desaturated gray pixels (those are typically background, text, shadows — not the brand)
- Among the remaining saturated pixels, identify the **dominant hue** — that's the brand primary color
- If a **second distinct saturated hue** is present, note it as an accent candidate (it will feed slot #4)
- On gradients, take the central hue, not the extremes
- If the logo is monochrome (one saturated color), use the monochrome derivation rules in Step 4

## Step 2: Optional brand-book lookup

After the initial analysis, try to **identify the brand** from the logo (visible text, distinctive shape, recognizable monogram).

If the brand is plausibly recognized, ask the user:

> "I think this logo is from **[brand name]**. Can I check their brand site (e.g., `brand.[brand].com`, `[brand].com/brand`, `design.[brand].com`) for official colors? (yes/no)"

**If the user says yes:**
1. Use `WebSearch` for queries like `"[brand] brand guidelines colors"` or `"[brand] brand book hex"`
2. Use `WebFetch` on the most promising URLs returned
3. Extract official hex codes from the page content
4. If you find official colors, use them as the source of truth for the palette
5. If the search/fetch fails (no brand site found, or no extractable hex codes), fall back silently to vision-only and mention: *"Couldn't find a public brand book, relying on the logo alone."*

**If the user says no, or the brand is not identified:** proceed directly to vision-only extraction.

## Step 3: Construct the 4-slot palette

Starting from the brand primary color (vision) or official colors (brand book):

- **Slot #1 (System navigation):** brand color in a deep/saturated form. If the brand is already dark, take it as-is; otherwise, darken it while preserving saturation.
- **Slot #2 (Window background):** an even darker version of #1 (same hue or a neighboring hue). Must be perceptibly darker than #1.
- **Slot #3 (Presence indication):**
  - Default: a saturated green close to Slack's native green (around `#20A271`)
  - If the brand has a secondary green, use it instead
  - Keep this slot green to respect the "online = green" convention
- **Slot #4 (Notifications / Selected items):** brand secondary accent, mid-light, saturated. This is what will pop in badges. If the brand has a second saturated color, use it; otherwise, derive via complement (+180° hue) or triad (+120° hue) of #1.

## Step 4: Monochrome logo derivation

If the logo has only one saturated color:

- #1 = the extracted saturated color
- #2 = a darker version of #1 (lightness reduced by 30-50%)
- #3 = default presence green (around `#20A271`)
- #4 = complement (+180° hue rotation) or triad (+120° hue rotation) of #1, mid-light tone

## Step 5: Validate the palette

Before producing output, check:
- Exactly 4 hex codes in `#RRGGBB` format (case-insensitive)
- The 4 colors are not strictly identical to each other

Do **not** check WCAG contrast — Slack adapts contrast internally.

## Step 6: Produce output

Output **exactly** this format. Re-emit it in full on every refinement (no partial diffs):

`````markdown
## Slack theme for [brand or short description]

```
#xxx, #xxx, #xxx, #xxx
```

| # | Slot | Hex | Note |
|---|---|---|---|
| 1 | System navigation | `#xxx` | short description |
| 2 | Window background | `#xxx` | short description |
| 3 | Presence indication | `#xxx` | short description |
| 4 | Notifications | `#xxx` | short description |

*Rationale*: 1-2 sentences on the logo reading (or brand-book source) and structural choices.
`````

## Refinement loop

After the first palette, the user may iterate with natural-language commands. Recognize these and **re-emit the full output (string + table + rationale) every time**. No partial diffs.

| User says | You do |
|---|---|
| "darker" / "lighter" | Shift the lightness of slots #1 and #2 |
| "less saturated" / "more saturated" | Adjust saturation, especially on #4 |
| "change the accent" / "warmer/cooler badge" | Re-pick slot #4 |
| "another angle" / "try again" | Re-read the logo, interpret the brand color differently |
| "light theme" / "dark theme" | Invert the structural strategy (light #1/#2 instead of dark, etc.) |
| "keep everything but the mention" | Keep #1, #2, #3 — change only #4 |

## Edge cases

- **No logo provided** (no path, no attached image): ask the user for the source (drag-drop, paste, or path)
- **Path given but file doesn't exist**: flag the missing file and ask for a valid path
- **File is not a readable image**: flag it and ask for a valid image
````

- [ ] **Step 3: Commit**

```bash
git add skills/slacque/SKILL.md
git commit -m "Add slacque skill with extraction, palette, and refinement rules"
```

---

## Task 4: Create the README

**Files:**
- Create: `/Users/rashadkaranouh/slack-colors-designer/README.md`

- [ ] **Step 1: Write `README.md`**

Create `/Users/rashadkaranouh/slack-colors-designer/README.md` with this exact content:

````markdown
# Slacque

Generate a custom Slack theme from a logo image. Produces a 4-hex string ready to paste into Slack's custom theme import.

## Install

```
/plugin install /path/to/slacque
```

Or use the Claude Code plugin menu.

## Usage

```
/slacque ~/Downloads/acme-logo.png
```

You can also drag-and-drop the logo into the terminal after typing `/slacque ` (with a trailing space), or paste an image (Cmd+V) after `/slacque` with no argument.

After the first output, you can refine conversationally:

- "darker" / "lighter"
- "less saturated" / "more saturated"
- "change the accent" / "warmer badge" / "cooler badge"
- "another angle" / "try again"
- "light theme" / "dark theme"
- "keep everything but the mention"

For recognizable brands, Slacque will offer to check the brand's website for official colors before producing the palette.

## Slack theme format

Slack accepts 4 hex codes in Preferences → Themes → Customize Your Theme → Import:

| # | Slot | Role |
|---|---|---|
| 1 | System navigation | Sidebar dominant color |
| 2 | Window background | Deeper background layer |
| 3 | Presence indication | "Online" green source (Slack derives a lighter shade) |
| 4 | Notifications | Badge and selected-item accent (Slack derives lighter shades) |

Example string: `#611F69, #39063A, #20A271, #C474D3`
````

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "Add README with install and usage docs"
```

---

## Task 5: Install the plugin locally and verify discovery

This task is interactive — run it inside a Claude Code session.

- [ ] **Step 1: Install the plugin**

In Claude Code, run:

```
/plugin install /Users/rashadkaranouh/slack-colors-designer
```

Expected: Claude Code reports the plugin was installed.

- [ ] **Step 2: Verify the plugin is listed**

In Claude Code, run:

```
/plugin
```

Expected: `slacque` (v0.1.0) appears in the installed plugins list.

- [ ] **Step 3: Verify the slash command is discoverable**

Type `/sl` in the prompt area.

Expected: `/slacque` appears in the slash command completion menu with the description "Generate a Slack theme from a logo".

- [ ] **Step 4: Note the result**

If any of the above failed, stop and debug. Common causes: missing `name` field in `plugin.json`, wrong frontmatter in `commands/slacque.md`, path typo in `/plugin install`.

If all passed, proceed.

---

## Task 6: Smoke-test the no-input path

- [ ] **Step 1: Run `/slacque` with no argument**

In Claude Code, type and submit:

```
/slacque
```

Expected: Claude (using the `slacque` skill) asks for the logo source — something like "Please share the logo. You can drag-and-drop a file, paste an image, or give me a path."

- [ ] **Step 2: Run `/slacque` with an invalid path**

Submit:

```
/slacque /tmp/does-not-exist.png
```

Expected: Claude reports that the file does not exist and asks for a valid path or another source.

- [ ] **Step 3: Note the result**

If Claude proceeds without flagging the missing file, the skill's edge-case section is being ignored. Review the SKILL.md edge cases section and tighten the wording if needed.

---

## Task 7: Test the happy path with a two-color logo

- [ ] **Step 1: Prepare a sample logo**

Save a clear two-color brand logo to a known path. Recommended candidates (download from the brand's site or use one you already have):
- Spotify logo (green + white/black)
- Stripe logo (purple + white)
- A logo of a Slack workspace you actually want to theme

Note the path, e.g., `~/Downloads/spotify-logo.png`.

- [ ] **Step 2: Run `/slacque` with the path**

In Claude Code:

```
/slacque ~/Downloads/spotify-logo.png
```

(If Claude offers brand-book lookup at this point, say "no" for this test — we'll test the brand-book path in Task 9.)

- [ ] **Step 3: Verify the output format**

Check that the output contains, in order:
1. A heading `## Slack theme for <brand or description>`
2. A code block with exactly 4 hex codes separated by `, ` (comma + space) on a single line
3. A 4-row table with columns `# | Slot | Hex | Note` and rows for System navigation, Window background, Presence indication, Notifications
4. A short `*Rationale*: ...` paragraph

- [ ] **Step 4: Verify the string format**

Read the 4-hex string. Verify:
- Exactly 4 codes
- Each matches `#RRGGBB` (6 hex digits, case-insensitive)
- Separator is exactly `, ` (comma + space)
- No trailing punctuation or whitespace

- [ ] **Step 5: Verify the slot semantics**

- Slot #1 should be a dark/saturated version of the brand color
- Slot #2 should be perceptibly darker than slot #1
- Slot #3 should be green (preserves the online-green convention)
- Slot #4 should be a mid-saturated accent

- [ ] **Step 6: Note the result**

If the format is off (missing rationale, wrong column count, extra text in the string), tighten the §"Step 6: Produce output" section of `SKILL.md` and rerun. Commit any skill changes.

---

## Task 8: Test the refinement loop

This task continues from Task 7's output in the same session.

- [ ] **Step 1: Refine "darker"**

Type and submit:

```
darker
```

Expected: Claude re-emits the full output (heading + string + table + rationale). Slots #1 and #2 are perceptibly darker than in the previous output.

- [ ] **Step 2: Refine "change the accent"**

Submit:

```
change the accent
```

Expected: Claude re-emits the full output. Slots #1, #2, #3 are unchanged or near-unchanged. Slot #4 is a different color.

- [ ] **Step 3: Refine "another angle"**

Submit:

```
another angle
```

Expected: Claude re-reads the logo (it may say so explicitly) and produces a noticeably different palette interpretation.

- [ ] **Step 4: Refine "light theme"**

Submit:

```
make it a light theme
```

Expected: Claude re-emits with light slots #1 and #2 instead of dark. The overall palette inverts its structural strategy.

- [ ] **Step 5: Note the result**

If any refinement command was ignored or partially applied (e.g., Claude diffs instead of re-emitting), tighten the §"Refinement loop" section of `SKILL.md` and rerun. Commit any skill changes.

---

## Task 9: Test the brand-book lookup path

Start a fresh Claude Code session (or `/clear`) so previous context doesn't bias the test.

- [ ] **Step 1: Use a recognizable big-brand logo**

Pick a logo from a well-known brand whose brand site is likely indexable (e.g., Spotify, Stripe, GitHub, Slack itself). Save it to a known path.

- [ ] **Step 2: Run `/slacque` and accept the lookup offer**

Submit:

```
/slacque ~/Downloads/spotify-logo.png
```

Expected: Claude proposes "I think this logo is from **Spotify**. Can I check their brand site (...) for official colors? (yes/no)".

Reply:

```
yes
```

Expected: Claude performs `WebSearch` and `WebFetch` (visible in the tool calls), extracts official hex codes, and produces the palette using those colors. The rationale mentions the brand-book source.

- [ ] **Step 3: Test the "no" path with another logo**

Start a fresh session. Run `/slacque` on a different logo. When Claude proposes the lookup, reply:

```
no
```

Expected: Claude proceeds with vision-only extraction and produces the palette without performing any web search.

- [ ] **Step 4: Test the fallback when search returns nothing**

Run `/slacque` on a logo of a less-indexable brand (a small company or a fictional one). When Claude proposes the lookup, say `yes`.

Expected: If the search/fetch returns no usable hex codes, Claude falls back silently to vision-only and mentions: "Couldn't find a public brand book, relying on the logo alone."

- [ ] **Step 5: Note the result**

If Claude never proposes the lookup for an obvious brand, tighten the §"Step 2: Optional brand-book lookup" section of `SKILL.md`. If Claude searches without asking, the consent step is being skipped — make the wording more explicit. Commit any skill changes.

---

## Task 10: Test the monochrome logo derivation

Start a fresh session.

- [ ] **Step 1: Prepare a monochrome logo**

Use a single-color wordmark or icon (a brand whose logo is just one saturated color on transparent/white). Example sources: a brand SVG you have, or render a colored text logo yourself.

- [ ] **Step 2: Run `/slacque`**

Submit:

```
/slacque ~/Downloads/monochrome-logo.png
```

(Skip brand-book lookup for this test — say "no" if asked.)

- [ ] **Step 3: Verify the derivation**

- Slot #1 = the brand's single saturated color (or a dark version of it)
- Slot #2 = perceptibly darker than #1, same hue family
- Slot #3 = green (default presence green, around `#20A271`-ish since there's no brand green)
- Slot #4 = a complement or triad of #1 (a noticeably different hue), mid-light tone

- [ ] **Step 4: Note the result**

If slot #4 is just a tint of slot #1, the monochrome rule wasn't applied. Tighten §"Step 4: Monochrome logo derivation" in `SKILL.md`. Commit any changes.

---

## Task 11: Test image attachment via paste

- [ ] **Step 1: Take a screenshot of a logo**

Use the OS screenshot tool to capture a logo from a webpage. The screenshot goes to the clipboard.

- [ ] **Step 2: Run `/slacque` with no argument and paste the image**

In Claude Code, type:

```
/slacque
```

Then before submitting, paste (Cmd+V). The image should be attached to the message.

Submit.

Expected: Claude uses the attached image (it should explicitly mention "I see an attached image" or similar) and produces a palette.

- [ ] **Step 3: Note the result**

If Claude asks for a path instead of using the attached image, the logo source resolution in `commands/slacque.md` is wrong. Tighten the source-detection wording. Commit any changes.

---

## Task 12: Real end-to-end validation in Slack

- [ ] **Step 1: Pick a real target**

Choose a Slack workspace you actually want to re-theme and the corresponding logo (from the workspace's website or brand page).

- [ ] **Step 2: Run `/slacque` end-to-end**

Generate a palette, optionally refining 1-2 times until you're happy with it.

- [ ] **Step 3: Copy the 4-hex string**

Copy just the line inside the code block (the `#xxx, #xxx, #xxx, #xxx`).

- [ ] **Step 4: Paste into Slack**

In Slack (desktop or web), open Preferences → Appearance → Custom theme → Import. Paste the string into the import dialog. Click Apply.

Expected: the Slack UI re-themes immediately. The sidebar uses slot #1 / #2 colors, the online dot uses a green derived from slot #3, and notification badges / selected items use a color derived from slot #4.

- [ ] **Step 5: Final check**

- Is the sidebar legible (text contrasts with background)?
- Is the active channel visible?
- Does the mention badge pop?
- Does the theme feel on-brand?

If yes — the plugin works for its primary use case. If no, capture which slot looks wrong, refine via the conversational loop, and iterate.

- [ ] **Step 6: Tag v0.1.0**

```bash
git tag v0.1.0
git push --tags
```

---

## Self-review notes (already applied)

- **Spec coverage:** Each of spec §3-§10 is implemented by Tasks 1-4. Spec §2.2 (slash command + skill rationale) requires no implementation. Spec §7 validation is implemented inside the SKILL.md (Step 5). Spec §6.6 refinement loop is verified by Task 8.
- **No placeholders:** Every step contains the actual content. File contents are inline and complete.
- **Type consistency:** Plugin name `slacque` is consistent across `plugin.json`, command file path `commands/slacque.md`, skill directory `skills/slacque/`, and skill frontmatter `name: slacque`.
- **Naming consistency:** Slot #4 is labeled "Notifications" in the output table (matching Slack's visible chip name) but described as "Notifications / Selected items (source)" in the explanatory tables — intentional, since slot #4 drives both chips.
